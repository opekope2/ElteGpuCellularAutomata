{
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Debug",
            "target": "${workspaceRoot}/bin/cellular_automata",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText",
            "preLaunchTask": "Build"
        }
    ]
}